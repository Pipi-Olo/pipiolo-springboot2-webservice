language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis Vi Server 의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: ".gradlew clean build"

# CI 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - Copy.H13@gmail.com